# AGENTS.md

## 职责

本文件是 AI 进入仓库后的第一入口，只保存最高优先级的授权边界、禁止事项和文档索引。项目事实、架构、风格、协作流程和可执行命令分别由下列文档维护，不在本文件形成第二套口径。

## 必读

进入仓库后，必须先完整读取：

1. `README.md`：项目定位、当前阶段、边界和文件地图。
2. `PROJECT_RULES.md`：项目特有且可验证的架构红线。
3. `STYLE.md`：工程、内容、命名和测试风格。
4. `AI_COLLABORATION.md`：任务分级、协作生命周期、证据和收口。

## 按需读取

- `DEV_WORKFLOW.md`：准备修改代码、安装依赖、启动或验证服务、执行版本控制或发布时读取；它是可执行命令的唯一来源。
- `REQUIREMENT_DEVELOPMENT.md`：新功能、跨模块改动、内容 Schema 或外部服务变化、多批次实施及发布任务开始前读取。
- `docs/PRODUCT.md`：判断产品范围、用户价值、指标与阶段路线时读取。
- `docs/DESIGN.md`：修改页面、组件、字体、图片、动效或无障碍行为时读取。
- `docs/ARCHITECTURE.md`：修改数据流、依赖边界、内容加载、搜索、表单或发布形态时读取。
- `docs/CONTENT_MODEL.md`：新增或修改内容、引用、插画资产、读者建议及商业披露字段时读取。
- `docs/REFERENCES.md`：核对本项目引用过的外部网页与用途时读取。

## 文档权威层级

- 强约束和授权边界以 `AGENTS.md` 为准，架构红线以 `PROJECT_RULES.md` 为准，工程风格以 `STYLE.md` 为准，AI 协作、证据和收口以 `AI_COLLABORATION.md` 为准。
- 项目定位、当前已接通主链路、稳定运行摘要和文件地图以 `README.md` 为准；启动、验证、Git、服务和发布命令只以 `DEV_WORKFLOW.md` 为准。
- 产品范围与阶段路线以 `docs/PRODUCT.md` 为准，视觉规则以 `docs/DESIGN.md` 为准，技术边界以 `docs/ARCHITECTURE.md` 与 `PROJECT_RULES.md` 为准，内容、引用与图片资产合同以 `docs/CONTENT_MODEL.md` 为准。
- 当前实现和运行事实必须按适用范围由现有代码、匹配测试、`README.md` 中的稳定摘要和执行前只读核查共同支持；证据不一致时停止并报告，不得选择方便的一项覆盖其他证据。
- 单项需求文档只负责已确认的目标业务合同、范围、决策、实施边界和验收标准；尚未实现或尚未验证的目标不得写成当前事实或已交付结果。
- `docs/requirements/` 下的阶段记录和执行快照用于追溯，不定义默认智能体规则，也不得覆盖根目录治理文档、领域合同、当前代码、匹配测试或最新运行现场。
- 易变化的远端、平台、配置、域名和发布状态即使已有文档记录，执行前仍须只读复核。

## AI 协作材料

- 仓库只保存稳定源码、内容、测试、项目说明、需求合同、运行说明和必要验证入口。
- 不保存聊天流水、临时推理、重复审查稿、浏览器截图、一次性日志或搜索结果副本。
- ComfyUI 模型权重、未经授权的参考图、探索废图和超大原始文件不进入网站代码仓库；可追溯清单与经批准的小型工作流可以进入稳定目录。

## 必须

- 写文件前说明具体文件及每个文件的修改目标；非平凡任务明确目标、不做范围、完成标准和验证方式。
- 事实、推断、设计选择和未验证风险分开表达；不得把推荐方案写成已经运行的现状。
- 只沿当前需求的直接链路修改内容、页面、组件、Schema、服务适配器、测试和文档。
- 选择能完成当前目标的最小实现，不为可能的会员、评论、社区或数据库提前增加抽象和空壳。
- 新增内容类型、稳定 ID、URL 规则、引用字段、商业披露、外部表单或分析事件前，更新对应需求文档。
- 任何事实性文化陈述都必须可追到来源；现代译本、网站、图片或研究文章被引用时，在正文附近标注出处。
- AI 插画必须有人工审校、替代文本、说明文字和可追溯资产清单；不得伪装成历史图像、文物或原典插图。
- 删除、覆盖、移动或替换文件前先只读确认精确目标与影响；不得动用户已有的范围外修改。
- 引入依赖、外部服务、运行配置、Cookie、邮箱收集、支付或用户数据前，说明隐私、授权和退出策略。
- 修改业务代码时，同步核对适用的内容或资产追溯与披露、构建期诊断、仅针对非显然约束的关键职责说明，以及匹配测试；完成汇报分别说明结果，不适用时说明原因。
- 修改后说明改了什么、验证了什么、没有验证什么、剩余风险、版本控制和发布状态。

## 多智能体边界

- 主代理可以把互不依赖的只读研究、方案比较和审查任务并行交给子代理。
- 子代理默认不得写项目文件、安装依赖、运行服务、访问真实写接口、提交、推送或发布。
- 所有文件修改由主代理统一落地并完成冲突、占位符、交叉引用和验证检查。

## 只读核查

- 只读核查允许读取与搜索文件、列举目录、查看版本状态与差异、读取日志和配置、访问公开网页及调用无状态查询接口。
- 只读核查不允许写文件、安装依赖、修改配置、启动或停止服务、写数据库、执行 Git 写操作、远端同步或发布。
- 只读澄清默认按“结论、必要事实与证据、风险或未决项、下一步停点”输出；无内容项省略。
- 只读修改计划默认按“目标与不做、新增或修改文件及职责、实施顺序、验证范围与风险”输出，并停在计划，不开始实施。

## 禁止

- 未经用户明确授权，不执行 `git add`、`git commit`、`git push` 或任何远端发布。
- 未经用户明确授权，不安装、升级或移除依赖，不修改密钥和运行配置。
- 未经用户明确授权，不启动、停止或重启本地及远端服务。
- 未经单独授权，不写数据库、调用真实业务写接口、提交读者表单、发送邮件或执行支付相关动作。
- 不将较低风险环境验证解释为生产发布授权。
- 不做与当前任务无关的重构、格式化、重命名、依赖调整或接口扩展。
- 不通过兼容分支、默认值、隐藏入口或静默 fallback 掩盖未确认的业务决定。
- 默认不在 Vercel、其他最终环境或远端工作区直接修改业务代码；部署、重启或线上排障授权不得推导出远端直接修改授权。若未来确需例外，必须先在 `DEV_WORKFLOW.md` 定义精确入口、范围、身份门禁、验证、留痕和回流策略，并由用户逐次授权。
- 不复制黑神话、Apple、WIRED 或其他参考品牌的商标、专有字体、角色造型、图片或页面源码。
- 不把生成式模型输出直接当作历史、宗教或文化事实。

## 默认允许

- 读取当前任务直接相关的文件、目录、公开文档和配置。
- 搜索文本、比较方案、指出风险和提出不改变状态的建议。
- 在用户已明确授权的文件范围内执行本地文档或代码修改，并运行匹配风险的本地只读验证。

## 发布授权模型

在受保护 CI/CD 与环境规则正式建立前，所有提交、推送、预览部署和生产发布都采用逐次人工授权。后续若改为受保护流水线预授权，必须先更新本文件和 `DEV_WORKFLOW.md`，写清触发者、分支保护、环境保护、审批和撤销方式。

## 字符集

- 中文工程文档与代码文本统一使用 UTF-8。
- 公开网站以英语为主；中文名称使用简体汉字并配拼音或通行英文名。
