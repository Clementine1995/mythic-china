# PROJECT_RULES.md

## 职责

本文件保存 Mythic China 特有、违反后会破坏内容可信度、资产可追溯性、静态架构或读者体验的红线。通用授权边界见 `AGENTS.md`，工程风格见 `STYLE.md`，执行命令见 `DEV_WORKFLOW.md`。

当前已完成 M2、M3 与 M4 本地产品实现；静态工程、内容与视觉 Schema、内容/视觉图校验、current resolver、review 输出及 public 纯 builder 的适用红线已经落为 Schema、静态检查或自动化测试。M4 的完成不表示已有 public artifact、`ready/published` 内容、远端预览或发布。M5 外部交互、M6 public artifact assembly 与发布候选 QA、M7 生产发布及发布后基线仍须在 `001-mvp-foundation` 关闭前按需求逐步落地，不能仅靠人工记忆。

## 项目定位

本项目是“静态内容核心 + 原创视觉 + 来源意识 + 外部服务承接少量交互”的英语文化出版网站。

负责：

- 组织可搜索、可关联、可核查的故事、人物、异兽、地点、体系指南和主题合集。
- 以经过人工审校的原创插画提升理解和品牌辨识度。

不负责：

- 建立完整学术数据库、统一宗教体系或唯一正典。
- 在 MVP 阶段承载账号、社区、支付权限、运行时 AI 或复杂后台。

## 架构红线

1. **静态内容核心。** 已发布文章和合集必须能在构建期生成完整 HTML；关闭 JavaScript 后仍可阅读正文、来源和基本导航。
2. **栏目是视图，不是新系统。** 人物、异兽、地点、故事和指南共用 `entries` 内容合同；新栏目通过 topic、collection 和查询组合扩展，不复制一套页面、Schema 或数据库。
3. **身份与导航解耦。** `entryId`、`sourceId`、`assetId` 和公开 slug 必须稳定且唯一；内容移动合集或改变标签不得自动改变公开 URL。
4. **传统类型明确。** 已发布内容必须区分 `myth`、`folklore`、`religion`、`literature` 与 `historical-legend`，不得把文学创作、地方传统和历史记录合并成单一事实。
5. **来源可追溯。** 事实性文化陈述、原文译意和版本差异必须关联存在的 source；引用外部网站时显示来源标题/机构、URL 和访问日期，图片还要记录许可证或权利状态。
6. **视觉资产可追溯。** 每张公开视觉资产必须关联 asset manifest 与所属 Entry、Collection 或全站位置；所有 factual/canonical 视觉主张必须关联 source/claim，纯装饰的 invented 氛围背景不得充当文化证据。资产必须按 accessibility mode 提供 alt 或空 alt，并具备适用的 caption、credit、AI disclosure 和人工审校状态。
7. **禁止运行时生图。** 网站构建和访问链路不得调用 ComfyUI、模型推理或提示词生成；模型权重、探索废图与未授权参考图不得进入网站代码仓库。
8. **动态能力隔离。** 表单、分析、邮件、广告、联盟和未来支付必须通过单一 adapter/component 边界接入；内容页面不得散落第三方 endpoint、脚本和密钥。
9. **读者数据最小化。** 浏览器端选题建议默认只提交 `pageId + requestedTopic`；`email + emailConsent` 必须可选并成对出现。`createdAt`、request ID、处理状态和归一化结果只能由供应商/服务端或内部流程生成，不接受客户端伪造。MVP 不建立用户画像、公开投稿页或跨站追踪。
10. **编辑与商业分离。** 学术/编辑来源不得因佣金排序；affiliate、sponsor、ad 和 paid product 必须使用独立字段与可见 disclosure，不能伪装成参考资料。
11. **可访问阅读优先。** 正文、来源和导航不得依赖动画出现；必须尊重 `prefers-reduced-motion`，提供键盘操作、可见焦点、语义标题、合适对比度和有意义图片文本。
12. **不复制参考品牌。** 黑神话、Apple、WIRED、SpaceX 等只用于抽取构图、留白、层级和动效纪律；不得复制商标、专有字体、角色造型、图片、文案或页面源码。
13. **依赖与客户端 JavaScript最小化。** 默认使用 Astro 组件和构建期数据；引入 UI 框架、客户端岛屿、数据库或认证必须有可观察需求和独立验收。
14. **统一品牌母体，栏目只作可控变体。** 同一设计版本内，栏目不得新增字体、导航、功能组件、引用结构或交互模型；只能通过批准的 `--realm-*` token、纹理、构图、一次 Hero 动效和同一字体家族内的标题参数表达差异。文章正文、来源、表单与无障碍行为在栏目之间保持统一。
15. **表现层不得污染内容合同。** 内容、来源、Claim、术语、稳定身份和 canonical URL 不得依赖组件实现名、CSS class、主题 token、断点或主题资产路径。全站 UI 可以在用户批准后整体替换，但必须保持这些不变量以及语义阅读顺序、静态输出和无障碍合同。

## 红线验证映射

| 规则 | 权威合同入口 | `001-mvp-foundation` 关闭前必须具备的验证 | 通过标准 |
| --- | --- | --- | --- |
| 1、13 | `docs/ARCHITECTURE.md` 的“架构原则”“推荐技术栈”“MVP 明确不做” | 静态构建测试、无 JavaScript smoke、客户端 bundle 审查 | 正文和来源为预渲染 HTML；无未经批准的全局客户端框架 |
| 2、3 | `docs/CONTENT_MODEL.md` 的“核心对象” | 内容 Schema 与 ID/slug 唯一性测试 | 新栏目无需复制 Schema；所有稳定 ID 和 slug 唯一 |
| 4、5 | `docs/CONTENT_MODEL.md` 的“编辑分类”和“引用合同” | 已发布内容引用完整性测试 | 枚举合法；source ID 存在；外部网页有标题、URL、访问日期 |
| 6、7 | `docs/CONTENT_MODEL.md` 的“视觉资产合同” | asset manifest 校验与仓库大文件检查 | 每张公开图可追溯；仓库无模型权重和未批准原图 |
| 8、9 | `docs/ARCHITECTURE.md` 的“动态能力边界” | 第三方脚本白名单、表单字段与隐私测试 | 第三方调用集中；不采集合同外字段；无密钥进入客户端 |
| 10 | `docs/PRODUCT.md` 的“商业化边界” | disclosure 组件与商业字段测试 | 编辑来源与商业链接分区，赞助/联盟清晰可见 |
| 11、14 | `docs/DESIGN.md` 的“无障碍硬约束”“统一品牌母体 + 栏目可控变体” | 自动化无障碍检查 + 键盘/减弱动效人工验收 + 跨栏目 token/组件审查 | 无阻塞缺陷；核心内容不因脚本或动画隐藏；栏目只覆盖批准的 `--realm-*`，无专属字体或功能组件 |
| 12 | `docs/DESIGN.md` 的“参考与原创边界” | 设计审查与资产权利状态检查 | 未使用参考品牌受保护资产或直接仿制页面 |
| 15 | `docs/ARCHITECTURE.md` 的“可替换表现层合同” | 内容/身份快照 + 语义、链接、静态输出和无障碍回归 | 视觉重构不要求改写内容文件、稳定 ID、证据关系、slug 或 canonical URL |

当前文档与工程命令见 `DEV_WORKFLOW.md`。`001-mvp-foundation` 关闭前，若上表中的“必须具备”项没有真实测试入口，该需求不得标记完成。

上表中的真实键盘、减弱动效、JavaScript 失败、故障注入、支持平台与最终制品性能/视觉验收属于 M6 release-candidate gate；M7 只承接生产身份、live smoke、回滚、线上地区复核与 RUM/p75。两者都不定义 M4 本地产品实现是否完成，也不应重复写入单条内容的 `ready` 状态。移动验证时点不降低通过标准。

## 变更规则

- 修改架构红线属于架构决策，不得作为普通内容或页面改动顺手调整。
- 放宽红线前必须说明原规则、目标规则、影响范围、迁移和验证变化，并获得用户明确确认。
- 不得通过删除测试、缩小扫描范围或增加隐形例外绕过红线。
