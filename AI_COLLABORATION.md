# AI_COLLABORATION.md

## 职责

本文件定义 AI 与人工协作时的任务分级、授权升级、执行边界、证据口径、临时产物和收口方式。架构、风格、内容合同和执行命令分别由 `PROJECT_RULES.md`、`STYLE.md`、`docs/CONTENT_MODEL.md` 与 `DEV_WORKFLOW.md` 维护。

## 任务分级

### 只读核查

允许读取、搜索、比较和解释本地文件与公开资料；允许查看版本状态、差异和历史。不允许写文件、安装依赖、修改配置、启动服务、提交、推送或发布。

### 本地小改动

允许修改当前需求直接相关的文档、内容、代码和测试，并执行本地静态检查、构建或隔离测试。

本地小改动不自动包含：

- 安装、升级或移除依赖。
- 启动长驻服务或浏览器端到端测试。
- 提交读者建议、发送邮件或调用任何真实写接口。
- Git 提交、推送、预览部署或生产发布。

### 本地运行与真实联调

以下行为需要在执行前说明环境、实际配置、状态影响、数据范围、停止条件和证据要求：

- 启动或重启本地开发服务。
- 运行浏览器自动化、跨系统端到端测试或第三方表单测试。
- 调用真实分析、邮件、对象存储、支付或其他外部服务。
- 写入非生产数据或触发可观察的远端状态变化。

### 数据、隐私与商业系统变更

收集邮箱、读者建议、Cookie、支付身份或行为数据，接入广告、联盟、会员与赞助系统，以及执行数据库结构或数据变更，都需要独立需求、数据最小化说明、保留/删除策略和逐次授权。不得从普通代码修改授权中推导。

### 预览与生产发布

预览部署和生产发布是两个独立等级。生产发布前至少需要：

- 当前源身份通过需求规定的完整验证。
- 预览环境完成视觉、内容、链接、性能和无障碍验收。
- 准备发布的源身份与已验收身份一致。
- 域名、表单、分析和邮件等生产配置分别通过身份门禁。
- 用户明确授权本次生产发布，或命中已书面定义的受保护流水线预授权条件。

## 标准协作生命周期

### 开始前

非平凡任务用简短说明明确：

- 本次目标：最终可观察结果。
- 不做范围：本轮明确不展开的能力。
- 完成标准：可以停止的可验证条件。
- 验证方式：计划运行的最小必要检查。
- 环境与影响：涉及真实资源时说明。

### 需求文档判断

出现新功能、内容或图片 Schema 变化、跨模块改动、外部服务、数据收集、多批次实施或发布时，必须基于 `REQUIREMENT_DEVELOPMENT.md` 建立需求文档。需求文档只保存稳定合同和当前证据，不保存聊天流水。

### 开发就绪门禁

进入编码或批量内容生产前必须确认：

- 目标行为、本期不做、废弃行为和不得改变行为清楚。
- 会改变实现结果的待确认项为“无”。
- 内容分类、URL、稳定 ID、引用和图片披露规则明确。
- 实际影响文件、外部服务和授权等级已界定。
- 工作区状态、运行时与环境身份按 `DEV_WORKFLOW.md` 通过。

### 执行中

- 一次完成一个可独立验证的实施单元。
- 每项修改必须对应目标或验收标准；不能对应的不做。
- 内容和视觉探索可以并行，Schema、页面与构建链路的写入由一个主实现线收束。
- 发现旁支问题时：阻塞当前目标或造成错误才纳入；体验增强、重构和未来功能记录为后续建议。
- 需要扩大范围、增加外部服务或处理真实用户数据时停止并请求确认。

### 收口条件

同时满足以下条件时停止当前需求：

- 用户要求的行为已经实现，或只读结论已经交付。
- 风险匹配的验证通过，或未验证原因已经明确。
- 没有本次修改直接引入的阻塞问题。
- 需求文档状态与实际实现一致。
- 剩余事项属于后续优化或独立需求。

## 证据口径

### 文档与内容

- 修改文件与关键结论。
- 模板占位符、交叉链接和 UTF-8 检查结果。
- 外部引用的标题、链接、访问日期与用途。
- 内容状态、来源记录和英文审校状态。

### 视觉资产

- `assetId`、关联内容 ID、主图和全部导出物。
- 实际使用生成或编辑工具时，记录工具、工作流摘要、模型与 LoRA 版本/哈希、许可证和参考图权利状态；未使用 ComfyUI 时不得创建空的 ComfyUI 字段。
- 人工修改、文化审校、文字审校、accessibility mode、alt / 空 alt、适用的 caption、credit 与 AI disclosure。
- 桌面和移动构图、响应式尺寸及压缩结果。

### 页面与构建

- 定向测试、相关回归、静态检查、构建和差异完整性结果。
- 关键页面的桌面与移动截图或正式入口验证。
- 链接、结构化数据、Sitemap、RSS、搜索与表单的适用证据。
- 性能和无障碍结果必须标明工具、页面、环境、日期和源身份。

### 部署

- 已验证源身份、预览环境身份和目标发布身份。
- 平台构建结果、实际域名、配置身份和只读可用性检查。
- 是否执行了表单写入、分析、邮件、支付或数据操作。

## 临时产物

- 一次性脚本、临时截图、Network 导出、日志副本和探索废图默认不提交。
- 未经授权的参考图、模型权重和超大原图不进入代码仓库。
- 可复用的工作流、资产 manifest 或验证脚本只有在用途稳定、入口明确并有维护责任时才进入项目。

## 收口输出

完成后必须说明：改了什么、验证了什么、没有验证什么、剩余风险、版本控制状态，以及是否提交、推送或发布。
