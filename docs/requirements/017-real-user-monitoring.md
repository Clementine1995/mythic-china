# 017 真实访问性能监测：开发与验收说明

## 0. 职责与状态

2026-09-15 最终发布状态：用户授权的 13 文件已提交为 `73e482a0647d28d420363fbfd2864c6ee0369c46`；从该 clean revision 重新验证并冻结 141 文件，创建新受保护候选并于 01:22:44 UTC 原样提升到正式域名，deployment 为 `dpl_5ivyuseZxGH4SjXYzETgyd1NbVJa`。正式匿名 141 文件的状态、MIME、长度和 SHA256 全匹配；候选/正式 Privacy 的窗口、endpoint、13 路径与精确脚本一致，桌面/手机宽度说明可读，无浏览器错误。receiver 已改为 true，Logs/Traces 保持关闭；新窗维护已自动推进到 `1789434095000`（01:01:35 UTC），无异常、零样本。

固定采集期仍为 `[2026-09-16T00:00:00Z, 2026-09-30T00:00:00Z)`，尚未开始；启用后的无状态检查为根 GET 404、缺 Origin GET 403、允许 Origin 的 OPTIONS 204，均空正文/no-store，预检只证明 CORS 通道。没有发送真实测量 POST，不把离线 24 场景、页面 DOM 或匿名字节检查写成实际 Network/INP/收数验收。完整观察期、真实传输与现有地区/设备/读者验收继续 pending，MVP 尚未收口。发布会话已清除凭据，临时服务已关闭；没有 Git 推送，发布后的状态文档按独立文档检查点维护，不改变线上制品身份。以下“实际结果”“继续执行”和 09-14 段落保留为发布前的阶段快照，当前状态以本段和运行现场为准。

2026-09-15 实际结果：取消旧窗及插入新窗各影响 1 行，随后读回两行；旧 start/end/maintenance 不变，新窗 start `1789516800000`、end `1790726400000`、maintenance `1789432510000`、invalid/snapshot 均 null，两窗测量均为 0。新窗维护值是 INSERT 时间，不能另算一次 Cron 证据。717 项测试、Astro 132 文件零诊断、public/review 构建及 24 个原始脚本场景通过；取消 SQL 另经 5 组内存 SQLite/重复执行验证。网站入口仅日期字节改变，Worker 运行包与 7541dd2 完全相同。owner 随后已授权本批本地提交，实际身份以 Git 历史为准；新 clean-source 制品与发布仍须独立验证；昨天的一小时 Vercel Token 已过期、会话已清除，需恢复身份后继续。以下 09-14 段落均为历史快照，不覆盖本轮结果。

2026-09-15 继续执行：owner 明确授权“触发并修复调度”。只读现场已看到 `0 * * * *` 在 09-14 23:01:23 UTC、09-15 00:01:36 UTC 连续 Success；后一次与 D1 maintenance `1789430496000` 完全一致。自动维护已证实，无需手动触发或临时提高频率。旧窗未发布客户端、receiver 关闭、测量 0，维护将其标记 collection-paused；不恢复旧窗采样，新窗口整体顺延为 `[2026-09-16T00:00:00Z, 2026-09-30T00:00:00Z)`。

本次必要取消合同：仅受信任运维在核对接收关闭、对应客户端从未启用以及零测量后，允许将未封存窗口标记 `cancelled-before-activation`，封存标准 LCP/INP/CLS 三项 n=0、p75=null 快照。取消前整行保存于执行回执；不删旧行、不改原起止和维护时间。取消 SQL 精确匹配旧 start/end、未封存及零测量，遇并发写入/已封存时零行退出；不覆盖真实样本。只有这个明确取消、已封存且仍为零测量的窗口可以不阻塞后续新窗；普通 NULL reason、普通失败、未封存或有测量窗口继续阻止重叠。开窗程序和生成 SQL 使用同一语义并测试 NULL 比较，避免把普通窗口误放行。该例外不代表曾采集的窗口可重开，也不让旧窗成为完整 14 日结果。

2026-09-14 最新执行状态：owner 已授权完整启用。D1 唯一未来窗口已成功写入（09-15 至 09-29 UTC），测量 0、invalid/snapshot 为 null；维护时间仍为 INSERT 的 12:35:37 UTC，13:00 后及 13:20 复查没有推进。接收 false，公网无状态 404/403/503 与空正文/no-store/CORS 已核验。clean 7418f77 的候选 dpl_Eigw53XwYJCMBickdv67PV2wHCds 已 READY、受登录保护，141 上传源 UID 与冻结字节一致；本地/云端候选的限定页面、配置及新增 Privacy 核验通过，正式域名尚未提升。自动审批仍要求手动生产维护的单独确认，已向 owner 提交；不能绕过或把未执行记为成功。详细证据、会话退出和恢复入口见 [DEV_WORKFLOW](../../DEV_WORKFLOW.md#rum-完整启用执行授权2026-09-14)。以下同日候选准备段落记录启用前快照，不覆盖本段。

2026-09-14 后续准备已完成：clean `7418f7714b1c182b59085d783a26639879e6e3a0` 重新通过完整 check 与 public/review 构建，141 文件的完整制品已冻结，精确身份见 [DEV_WORKFLOW](../../DEV_WORKFLOW.md#rum-clean-source-发布包与维护核查2026-09-14)。707 测试、132 文件零诊断及 24 个原始脚本共存场景来自本次实际执行；尚未完成浏览器和真实云端验收，`releaseReady=false`。后续刷新 Cron Events 仍无记录，已不能只用 30 分钟延迟解释；Metrics 的 18 次调用/0 错误不能区分 scheduled。维护执行保持未证实，开窗写库授权与处理条件补充正在等待 owner，不开启接收或发布。

2026-09-14 启用候选准备：owner 要求合并推进网站接线、Privacy、14 日窗口及验证。当前本地基线为 clean `7541dd2d29c1506a30b6ceeeef89953aec2c6910`；Worker 新包为 6,959 bytes / SHA256 `24127d5e780179a26e805c2dba70fe61817cc59ec580ca393698b9bb188255d3`。控制台已显示 Active/Latest `4a9a22b8`、零诊断，HTTP 面板 GET 根路径为 404/空正文/no-store；该面板不能独立证明公网部署的完整源码身份。主代理已核对 DB 绑定到给定 UUID、RUM_ENABLED=false、Logs/Traces 主开关关闭且无导出目标；日志子选项在关闭时隐藏，不把隐藏勾选当作已启用或逐字段已核验。owner 回报两表计数均为 0。主代理后续已直接核对每小时 Cron 已保存、账户 Free 为 Current plan；Cron events 暂无记录，界面提示新任务最多延迟 30 分钟展示，实际执行仍待核验。

本批只准备本地启用候选，暂拟 `[2026-09-15T00:00:00Z, 2026-09-29T00:00:00Z)`；错过开始前的全部门禁时必须整体顺延。配置、公开 Privacy 日期、精确脚本/分块摘要与导入关系、原始制品离线执行须一起验证；review 仍零 JS，未知脚本/配置仍拒绝。无需修改 Worker 或 Schema，不重跑建表。适用处理条件/地区及必要 consent、维护实证和真实传输/退出仍为正式启用门禁，不因本地配置或下文候选通过而记为完成。owner 随后已授权本批本地提交；开窗写库、开关和发布仍未授权执行，本批不采集真实数据。第 6、7 节为历史快照，当前候选及后续实际结果以本段、DEV_WORKFLOW 和运行现场为准。

2026-09-14 历史版本与运行边界：已发布网站仍绑定 `21cdbb6353bb14fe5ebced9ddfec1e94dc30a3a5`，其 RUM 配置为 null。网站启用候选父提交为 `7541dd2d29c1506a30b6ceeeef89953aec2c6910`，本地检查点已获授权，实际提交以 Git 历史为准；网站候选尚未发布。候选已接入真实 endpoint 与拟定日期、启用版 Privacy、两份精确锁定 ESM；707 项测试与公开输出的 24 个隔离执行场景通过，review 零 JS。精确资源、SQL 制品与执行入口见 [DEV_WORKFLOW](../../DEV_WORKFLOW.md#rum-网站启用候选准备2026-09-14)。本地候选和资源创建均不代表真实 RUM 接通；站点发布身份保持独立。

本文负责 006 第 5.5 节 RUM 的本地实现及后续接通条件，不负责其他三地区浏览器、真机、读者研究或发布验收。

| 维度 | 当前状态 | 依据 |
| --- | --- | --- |
| 需求 | 本地实施及后续启用分别获确认 | 09-10 先实施本地版本；09-14/15 后续授权创建资源、修复窗口及启用，具体范围见 DEV_WORKFLOW，既有授权不构成以后持续写入许可 |
| 实施 | 73e482a 已提交并正式发布，接收开关开启 | 同一冻结包提升到正式域名，dpl_5ivyuseZxGH4SjXYzETgyd1NbVJa；141 公开文件逐字节匹配 |
| 验证 | 本地自动化通过，真实 RUM 未验收 | 本轮 42 文件/717 测试；Astro 132 文件零诊断；public 24 个 RUM 共存场景及 GoatCounter 回归通过；review 14 页零 JS |
| 线上 RUM | receiver=true，客户端已发布，等待固定窗口开始 | 09-16/09-30 UTC；新窗 maintenance=1789434095000、invalid=null、零测量；旧空窗取消封存。预检 204 不代表真实收数 |

2026-09-10 首次实施基线为 `29b3d953588d5f9dcb7b37d426a99a8760f22310` 加当时技术验收文档与 Entry Hero sizes 修改；历史结果见第 6 节。当前本地与线上身份分别以上述 Git 和发布回执为准。

## 1. 目标、范围与授权

本地开发就绪：是。实现 Google web-vitals 的 LCP、INP、CLS 采集、严格接收、乱序去重、固定窗口 p75、保留清理和失败关闭。新增精确锁定的 `web-vitals@6.2.1` 是本次实现的唯一运行依赖；库不加载远程脚本、不自行上传数据。Cloudflare 是当前已部署的接收处理方，网络层仍可能处理 IP 与请求头，应用代码不保存这些信息。

不增加页面/设备分析、用户画像、Cookie、浏览器存储、通用后端、仪表盘、账号或通知。Worker 与 D1 仅用于性能测量，正文继续静态。2026-09-10 本地实施授权只覆盖源代码、依赖锁定、默认关闭配置和离线验证；后续 Git 检查点另行授权。本文不提供创建 Cloudflare 资源、接受条款、真实写库、启动服务、推送或部署的持续授权。

退出策略为关闭客户端构建接线和接收开关，验证零请求，再按精确授权删除活动测量及核对供应商恢复/备份处理。没有账户时不编造 endpoint、数据库 ID 或发布配置。

## 2. 数据与统计合同

- 每次请求只含 `name`（LCP/INP/CLS）、有限非负 `value`、随机 UUIDv4 `measurementId` 和正整数 `sequence`；最大请求体 512 bytes，拒绝额外字段。LCP/INP 单位为 ms，CLS 为分值。不发送库原始 ID（含时间信息）、navigationId、entries、URL、referrer 或 attribution。
- 同一库指标实例只在内存映射到一个随机 ID；重复数值不重复发送，新回调增加序号。BFCache 新实例得到新随机 ID。服务端只接受更高序号，允许数值降低；不把缺失 INP/浏览器不支持变成零。
- 客户端仅在已配置的 public 构建、真实正式 origin、已发布路径、固定采集日期内运行。`navigator.webdriver`、DNT/GPC、含 `rum=off` 的查询或片段均禁发；维护/自动化先使用此禁发入口。后续加载与每次发送重新检查。不能把 CORS/webdriver 当作真人身份验证。
- 样本为指标实例，不是独立读者。窗口预先选为 UTC 00:00 开始的连续 14 个完整自然日；仅在 `[start, end)` 接收，以首次接收归窗。截止后拒绝更新，SQL 写入也重查窗口是否已封存，防止跨截止的在途请求倒改结果。
- 每指标去重 `n >= 50` 才给出 nearest-rank p75（排序第 `ceil(0.75*n)` 项），不足返回明确缺样和 null，绝不补零。每个窗口封存一次，后续只读同一聚合快照。
- 每小时维护：清理按首次接收已满 30 天的活动数据（准时执行时延迟最多约一小时）；冻结到期窗口。维护间隔超过 2 小时、维护时发现窗内接收开关关闭、已知数据库失败将窗口标为不完整；窗口不能因此报告通过。首次数据库查询失败可能无法写入失效标记，两次维护间短暂停用也可能无自动留痕；这些情况、平台未执行请求和未知机器人均需实际平台错误/维护记录核查。因此有足够样本的本地计算结果仍标记“需要运行复核”，不自动宣布 Beta 达标。
- HTTP 204 只表示请求已处理；重复、乱序或数据库截止排除不产生新样本。`n` 仅取数据库去重统计，不能从发送次数或 204 次数推算。
- Worker 不输出原始正文、请求头或异常细节；关闭持久日志/invocation logs、禁用 Tail/Logpush 是持续运行约束。活动清理不等于供应商即时物理删除；D1 Free Time Travel 另有恢复期限，实际账户需复核。

## 3. 实现边界与顺序

1. `src/rum/contract.ts`、`configuration.ts`、`collector.ts`、`bootstrap.ts`、`web-vitals.ts`：字段、可关闭配置、环境门禁、唯一发送及延迟自托管库加载。`astro.config.mjs` 与 `SiteLayout.astro` 仅在配置非空的 public 构建接线；当前已发布 public 接线非空，review 保持关闭，null 分支不增加 RUM 接线。
2. `workers/rum/schema.sql`、`store.ts`、`worker.ts`：独立 D1 最小表、条件 upsert、SQL 聚合、冻结与清理；仅 POST `/vitals`，无公开数据/管理写接口。开窗由受信任运维调用，不能由访客触发。
3. `tests/rum/`：注入 Fake callbacks/fetch，使用 Node 内存 SQLite 执行真实 SQL，零网络/无持久数据库；补窄架构允许项。当前输出 verifier 保持拒绝未审 RUM bundle，不能改配置绕过发布门禁。
4. README、ARCHITECTURE、006/011、DEV_WORKFLOW 与 Privacy 同步已发布接线、固定观察窗和真实联调待验范围；源内容、图片、字体及其追溯不变。

## 4. 验证与完成标准

定向验证 off/review/preview/未知路径/维护/自动化/偏好禁发；严格字段/大体积/失败；重复和乱序、BFCache ID 重置；14 天和截止竞争；49/50 样本、p75、缺样、快照不可变、清理和维护中断。完整 check 与 public → review 构建须通过。当前启用候选精确锁定页面入口和延迟 chunk 的两文件库存、摘要和导入图，实际原始 ESM 须通过共存与隐私禁发执行检查；Privacy 日期与客户端独立核对。null 分支仍不输出 RUM，未知资源不得借关闭/启用放宽。命令只见 DEV_WORKFLOW。

截至 2026-09-15，账户 Free 计划、既有处理条件说明、精确日期/endpoint、Privacy、两份 ESM、clean 73e482a 制品、D1 未来窗、receiver=true及小时维护已有各自回执。窗口已建立，发布时尚未到 09-16 UTC 起点；这些发布和维护步骤不再列为未实施，也不应为补文档重开窗口或重发候选。处理条件与地区证据的适用范围继续见 006，不据此外推为所有读者场景已取得必要 consent。

仍未闭合：真实 Network/INP与去重收数、真实隐私/维护禁发和退出清理、完整窗口的封存与运行错误复核，以及 Worker 实际运行兼容/额度拒绝等尚无充分实证的场景。补证时按 DEV_WORKFLOW 明确环境、读写范围和停止条件；本地 Fake/内存数据库通过不替代这些运行结果。只有完整有效窗口且每指标 n >= 50 时才报告 p75；完整窗后流量不足的接受限制继续按 011，不降低窗口/样本门槛，也不宣称性能通过。

## 5. 外部依据

2026-09-10 核对：[Google web-vitals v6.2.1](https://github.com/GoogleChrome/web-vitals/releases/tag/v6.2.1)、[库接口及生命周期](https://github.com/GoogleChrome/web-vitals)、[D1 prepare/batch 事务](https://developers.cloudflare.com/d1/worker-api/d1-database/)、[Cron](https://developers.cloudflare.com/workers/configuration/cron-triggers/)。价格、隐私和退出依据继续见 006 第 5.5.1 节；它们不证明实际账户已配置。

## 6. 本地实施记录（2026-09-10 历史快照）

2026-09-10 本地实现完成。新增 35 项 RUM 测试覆盖字段裁剪、默认关闭/preview/自动化/维护/隐私偏好、单次初始化、重复/乱序/BFCache、传输/观察器异常、49/50/51/100 样本、缺失指标、SQL 截止竞争、封存、30 天删除和维护回滚。独立只读审查发现观察器部分注册失败后仍可能发送，已将失败锁扩至初始化全程，并补回归；另补 429 后停止发送测试。最终完整 check 41 文件/690 项通过，Astro 128 文件零 error/warning/hint。

显式 origin 的 public 构建与返回 review 构建均通过。public 仍为 13 HTML、2 XML、robots、112 Hero、10 字体、原唯一 `page.Di-gmpYO.js`，合计 140 文件；原脚本 SHA256 和 VM 离线执行验证通过，未产生 RUM meta/入口/chunk。与原冻结上传包逐文件比较，仅六篇 Entry 的既有 sizes 修改及本批 Privacy 不同，其余 133 文件字节相同；review 14 页仍零客户端 JS。

内容/资产配套：未改文化内容、引用、manifest、图片、字体、alt/图注/AI 披露；本批公开披露只新增 RUM 未启用计划。构建诊断通过，关键注释只解释延迟加载、失败关闭、随机实例、数据库截止及封存先于清理；匹配测试已通过。不适用视觉资产生产，未运行服务/浏览器或真实 Worker，未验收浏览器实际 Web Vitals 事件、Cloudflare runtime/配额/网络与条款；本地 SQLite/Fake 不替代这些证据。

依赖只新增 web-vitals 6.2.1，无传递依赖或其他包升级，registry integrity 与锁文件一致，未运行安装脚本。原技术验收文档与 Hero sizes 修改保留。源码与文档未暂存、提交、推送或发布；无 Cloudflare 账户/资源/调度和真实测量数据。

文档收尾通过 38 份 Markdown 严格 UTF-8、100 个相对链接、当前修改范围占位符/旧状态检查与 diff 空白检查；链接检查排除 fenced/inline code，不把 PowerShell 类型转换当链接。暂存区为空。

本地实施单元可关闭；线上 RUM 与 Beta 验收保持开放。下一项是账户准备后核对 Free 计划与处理条件，实例化接收端、D1、日志/调度/开窗/退出入口，完成受控真实联调，再准备精确启用制品和逐次发布。无需为了继续其他本地工作先注册账户。

## 7. 关闭状态接收端准备（2026-09-14 历史快照）

后续编辑器实证：Quick Edit 报 `TextDecoderConstructorOptions` 缺少 `ignoreBOM`，旧候选暂停发布。已在本地显式补齐标准默认值 `false`，保持 `fatal: true`，并补带 BOM 的 JSON 回归；RUM 3 文件/36 测试、定向格式/ESLint 与 Astro 128 文件零诊断通过。owner 已明确授权修正、回归与三份运行文档共五文件的本地提交，以及新 clean-source 候选打包；入口与精确范围由 DEV_WORKFLOW 维护，结果以 Git 历史及新 manifest 为准。尚未确认云端错误清除，不把 Node 本地检查写成 Cloudflare 类型验证已通过。

初次打包时，owner 按手工步骤完成账户、Worker、D1 建表和关闭开关/日志的准备；主代理只生成本地接收端制品和操作说明，没有操作真实写接口。现有 esbuild 将三个源码模块打包为仅默认导出的单文件 ESM，未引入依赖或修改业务源码。该次 RUM 3 文件/35 测试通过；原制品原始字节离线执行确认关闭或缺绑定时零数据库访问、无窗口时拒收、根路径 404、无 Origin 的 `/vitals` 403，以及零外发网络。后续源码修正与验证见上一段。

本轮仅交付关闭状态的手动部署候选，精确字节、摘要与步骤由 DEV_WORKFLOW 维护；尚未验收 Cloudflare runtime、真实绑定、最终账户配置或接收端部署。`scheduled` 在接收关闭时仍执行维护 SQL，本轮因此不配置/触发 Cron。开窗、真实采集、客户端/Privacy 与 Vercel 制品更新仍是后续工作；仅数据库建表不会启动 14 日计时。

内容、来源、资产及公开披露未改，无新增相关审校项；仅执行匹配的 RUM 测试与制品检查，不重跑站点全量构建。忽略目录候选不进入 Git，本地检查点不包含推送或由代理发布的授权。
