# 015 墨色背景与暖色阅读体系

## 目标与授权

2026-09-08，owner 指出 014 只调整版式、整站背景仍缺少神话气氛，明确希望向 mythzh 的整体背景靠齐，并重新核查产品优势。本批在当前保存项目和未提交工作树中完成可回看的深色版本；沿用同一设计任务中已授予的临时 preview、浏览器检查及结束后关闭授权。没有 Git、发布、依赖或外部写入授权。

设计目标已确认；本地实现及本轮自动化/浏览器核查完成，尚未取得 owner 对成品的最终视觉确认，未发布。源基线为 `fabb4c9e829dc85c6c47161f49af5e9ad58594bd` 上已有 013/014 的本地修改；不覆盖这些修改。

## 事实、设计选择与范围

- 参考事实：[mythzh 中文首页](https://mythzh.com/index.html) 的实际浏览器画面与计算样式显示墨黑背景、暖白文字、金色细节，以及融入暗部的主视觉。它已有英文版和来源分层，不能把这些称为本站独有优势。内容样本和局限见 [竞品审计](../COMPETITIVE_AUDIT.md)。
- 设计选择：共享背景改为微暖墨色，配暖白正文与克制金色链接/强调；表面用三档深色形成层次。继承 014 的版式与八组自有插画，保留两专题的独立视觉。
- 范围：`src/styles/global.css` 色板及必要对比修正、`src/layouts/SiteLayout.astro` 的原生深色声明；同步 DESIGN、PRODUCT 的相关判断、竞品引用、本需求、README 与 DEV_WORKFLOW 验证摘要。
- 不涉及内容、来源、术语、URL、Schema、原插画、裁切、字体、客户端脚本、表单或部署。借鉴色彩关系，不复制参考站素材、书法、标识、装饰或源码。不增加主题切换器。

## 完成标准与验证

1. 首页、目录、专题、文章与功能页采用一致色板，正文不垫纹理；检查主文、次文、图注、按钮和焦点的实际对比。
2. 现有配套测试、review/public 构建和各自输出校验继续通过；不为颜色常量新增镜像测试。
3. 临时 preview 抽查桌面与手机首页、两个专题、目录、长文正文/来源及功能页；样式截图存仓库外。颜色改变使旧配色证据失效，不复用旧截图作为本次通过证明。
4. 检查完成关闭本批 preview 并复核监听状态；准确记录未覆盖项。读者舒适度、真实设备、跨平台、完整故障矩阵与最终发布候选验收仍需后续完成。

## 竞争判断边界

本站当前为六篇、两个专题，尚无真实目标读者反馈。已有差异是部分文章提供更直接的出处链接与精确定位；这不证明全站更权威、更好看或更易读。聚焦少量故事、提升阅读吸引力是需要验证的方向，不是已成立的竞争壁垒；本批不据此擅自改选题和正文。

## 实施与验证记录

2026-09-08，本轮代码仅修改共享九项色值、根节点 color-scheme 与页面 meta。首页、目录、两个专题、统一文章阅读器和功能页共同采用墨色体系，未重新制作图片或改变 014 的布局。独立静态审查发现的原 light meta 已同步修复；独立截图审查确认新色板与现有画面协调。

- 完整 check 通过格式化、ESLint、既有测试、Astro 检查和 review build/output verifier。随后 public → review 两种构建均为零 error/warning/hint；public 13 HTML、2 XML、robots.txt、112 Hero、10 字体，review 14 noindex HTML、112 Hero、10 字体，均保持零可执行脚本。没有新增颜色镜像测试。
- Codex In-app Browser 检查 1440×900、390×844 下的首页、Explore、Collections、两个专题、Painted Skin、About、Privacy，共 16 个页面组合。实际 DOM 均为 dark scheme，未发现横向溢出，字体及各页图片加载完成。桌面首页/专题区域、手机首页/聊斋、画皮正文/手机 Sources 和菜单键盘焦点另有实际截图审阅；不是每个组合都进行了全长页面视觉检查。
- 共享色板按相对亮度核算：正文在三个深色表面上的最低对比度为 12.34:1，次文为 6.85:1，金色链接为 7.87:1；深色字/金底按钮为 8.92:1。手机菜单 Tab 到 Home，实际金色 2px 焦点环与 4px offset 可见；当前检查标签无 console error/warn。此核算不替代复杂图片叠字、真实设备和读者舒适度验证。
- 保留原 alt、caption、credit、AI disclosure、来源定位与链接，沿用并通过原输出/内容/字体门禁。没有新增文化陈述、资产、服务、配置或依赖；不新增无必要职责注释。低对比分隔线仅作装饰，未用作焦点。
- 临时 preview PID 32920，实际监听 [::1]:4321；结束后已通过固定 stop 入口停止，PID 不存在且 4321 无 LISTENING。临时 viewport 已重置，两个本轮检查标签已关闭。截图和检查 JSON 保存在仓库外的 visualizations/ink-palette 目录。

保留限制：手机完整图注仍偏长；本轮未更改披露文案或折叠行为。浏览器检查为 review 版，public 专属日期/Beta 提示仅通过静态验证。未重跑全量故障/慢加载/真实缩放、全部 Entry 的新视觉截图、平板、读屏器、跨平台、性能或目标读者比较；旧配色的相关证据不视为新版通过。此次为 dirty source 本地诊断，没有 clean-source receipt、Git add/commit/push 或远端发布。
